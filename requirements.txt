# BBH-AI Python Dependencies — pinned for reproducible installs.
# Install: pip install -r requirements.txt

# ── AI / Agent Framework ─────────────────────────────────────────────────────
crewai
langchain
openai        # Used for OpenAI AND DeepSeek (OpenAI-compatible API)
anthropic
google-generativeai

# ── Config & .env support ─────────────────────────────────────────────────────
pyyaml
python-dotenv   # Loads .env file into environment variables
pydantic
celery
redis

# ── Sandbox / Container ───────────────────────────────────────────────────────
docker
fastapi
uvicorn[standard]

# ── HTTP / Scraping ───────────────────────────────────────────────────────────
requests
urllib3

# ── Reporting ─────────────────────────────────────────────────────────────────
jinja2

# ── Technology Detection ──────────────────────────────────────────────────────
python-wappalyzer
lxml
beautifulsoup4>

# Development / testing
pytest