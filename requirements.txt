# BBH-AI Python Dependencies — pinned for reproducible installs.
# Install: pip install -r requirements.txt

# ── AI / Agent Framework ─────────────────────────────────────────────────────
crewai>=0.30.0
langchain>=0.1.0
openai>=1.0.0        # Used for OpenAI AND DeepSeek (OpenAI-compatible API)
anthropic>=0.7.0
google-generativeai>=0.3.0

# ── Config & .env support ─────────────────────────────────────────────────────
pyyaml>=6.0
python-dotenv>=1.0.0   # Loads .env file into environment variables
pydantic>=2.0.0
celery>=5.3.0
redis>=5.0.0

# ── Sandbox / Container ───────────────────────────────────────────────────────
docker>=6.0.0
fastapi>=0.100.0
uvicorn[standard]>=0.23.0

# ── HTTP / Scraping ───────────────────────────────────────────────────────────
requests>=2.31.0
urllib3>=2.0.0
httpx>=0.24.0

# ── Reporting ─────────────────────────────────────────────────────────────────
jinja2>=3.1.0

# ── Technology Detection ──────────────────────────────────────────────────────
python-wappalyzer>=0.4.0
lxml>=4.9.0
beautifulsoup4>=4.12.0

# ── Browser Automation ────────────────────────────────────────────────────────
playwright>=1.40.0

# ── Database & Storage ────────────────────────────────────────────────────────
aiosqlite>=0.19.0

# Development / testing
pytest>=7.4.0
pytest-asyncio>=0.21.0
black>=23.0.0
flake8>=6.0.0
mypy>=1.5.0